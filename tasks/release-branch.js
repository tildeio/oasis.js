module.exports = function (grunt) {
  // TODO: clone repo in tmp
  // checkout branch
  // copy bower.json & dist/*
  // make commit
  // force push

  grunt.registerTask('release-branch', 'push the built files in the workspace to the dist repository, under a branch that matches the current branch name.', function () {
    
  });
};
